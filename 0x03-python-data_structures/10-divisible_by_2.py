#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    new_list = []
    for j in len(my_list)]:
        if my_list[i] % 2 == 0:
            nw_list.append(True)
        else:
            nw_list.append(False)
    return nw_list
